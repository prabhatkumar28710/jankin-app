pipeline {
    agent {
        docker {
            image 'node:latest'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'node -v'
                echo 'Building the React app2...'
            }
        }
    }
}